{
  "name": "fd-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Fixed Deposit account creation UI (plain HTML/CSS/JS, no build step)",
  "scripts": {
    "start": "node server.js"
  }
}
